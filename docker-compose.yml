services:
  marzban:
    build: .
    restart: always
    env_file: .env
    ports:
      - "8000:8000"
    environment:
      - DEBUG=false
    command: bash -c "alembic upgrade head; uvicorn main:app --host 0.0.0.0 --port 8000 --workers 1"
    volumes:
      - /var/lib/marzban:/var/lib/marzban
